apiVersion: v1
kind: Service
metadata:
  name: edge-service
  namespace: nginx-ingress
spec:
  type: LoadBalancer
  selector:
    app: nginx-ingress
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 80