apiVersion: v1
kind: Service
metadata:
  name: metadata-postgres-service
  namespace: backend-veyor
spec:
  type: ClusterIP
  selector:
    app: metadata-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432