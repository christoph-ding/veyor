---
apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: backend-veyor
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - protocol: TCP 
      port: 3000
      targetPort: http-grafana
